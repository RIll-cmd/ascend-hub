import { useEffect, useRef } from "react";
import type { Layout } from "./storage";
type Context={registerTool:(tool:{name:string;description:string;inputSchema:object;annotations:object;execute:(input:unknown)=>unknown},options:{signal:AbortSignal})=>void|Promise<void>};
export function useWorkspaceTools(layout:Layout){const latest=useRef(layout);useEffect(()=>{latest.current=layout},[layout]);useEffect(()=>{const context=(document as Document&{modelContext?:Context}).modelContext;if(!context?.registerTool)return;const lifecycle=new AbortController();try{Promise.resolve(context.registerTool({name:"read_workspace_layout",description:"Read the current Ascend OS layout, editable labels, local media references, and milestone completion status.",inputSchema:{type:"object",properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},execute(input){if(!input||typeof input!=="object"||Array.isArray(input)||Object.keys(input).length)throw new Error("Expected an empty object");return JSON.parse(JSON.stringify(latest.current))}},{signal:lifecycle.signal})).catch(()=>{})}catch{/* Feature detection keeps unsupported browsers functional. */}return()=>lifecycle.abort()},[])}
